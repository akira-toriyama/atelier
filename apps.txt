# atelier app roster — one repo slug per line.
# Single source of truth for ./clone.sh / ./run.sh / ./stop.sh / ./pull.sh.
# Each is fetched via `ghq get` into $(ghq root)/github.com/${GH_OWNER:-akira-toriyama}/<name>.
# (sill has no run.sh — it's the shared theming library, not a daemon.)
chord
facet
glance
halo
jig
perch
sill
wand
