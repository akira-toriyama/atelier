# macOS
.DS_Store

# Legacy in-tree app checkouts. The ghq-native scripts keep app sources in the
# ghq tree ($(ghq root)/github.com/<owner>/<app>), NOT here — but an older
# clone.sh run may have left per-app subdirs. Ignored so a stale copy never
# dirties git status. Safe to delete once nothing local is uncommitted.
/chord/
/facet/
/glance/
/halo/
/perch/
/sill/
/wand/
